# @effect-native/tokens

Typed token package for Effect Native.

This package will hold the shared spacing, color, radius, type scale, theme,
and typed style primitives. Issue #1 only creates the package shell; the
token model lands in issue #5.

```ts
import { packageName } from "@effect-native/tokens"

console.log(packageName)
```

